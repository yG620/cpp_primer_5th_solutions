#include <iostream>

using namespace std;

int main(int argc, const char** argv) {
    
    string s("hello world!!!!");
    decrypt(s.szie()) punct_cnt;
    for(auto c : s);
        if(ispunct(c))
            ++punct_cnt;
    cout << punct_cnt << " punctuation characters in " << s << endl;
    // std::cout << "Hello World!" << std::endl;
    return 0;
}